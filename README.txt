This project currently uses free hosted placeholder photos (Unsplash URLs)
defined directly in script.js (the `image` field of each product in PRODUCTS).

To use your own photos instead:
1. Put your image files in this /images folder (e.g. images/coat.jpg).
2. In script.js, change the matching product's "image" value to: "images/coat.jpg"
